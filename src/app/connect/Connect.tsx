'use client';

import { NAV_LIST } from '@/components/nav/nav';

export default function Connect() {
  return (
    <div id={NAV_LIST[6].id} className="page-container connect">
      <div id="connect-page1" className="-z-10 h-screen w-screen" />
    </div>
  );
}
