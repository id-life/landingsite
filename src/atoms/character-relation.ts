import { atom } from 'jotai';

export const isBePartOfItShowAtom = atom<boolean>(false);
export const isBePartOfItSubmittedAtom = atom<boolean>(false);

export const isMobileCharacterRelationShowAtom = atom<boolean>(false);
export const isMobileBePartOfItShowAtom = atom<boolean>(false);
export const isMobileBePartOfItSubmittedAtom = atom<boolean>(false);
