import { atom } from 'jotai';

export const isSubscribeShowAtom = atom<boolean>(false);
export const isMobileFooterContactShowAtom = atom<boolean>(false);
export const subscribeTypeAtom = atom<string | null>(null);
